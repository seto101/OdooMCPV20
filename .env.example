# ====================================
# Odoo Configuration (REQUIRED)
# ====================================
ODOO_URL=https://your-odoo-instance.com
ODOO_DB=your_database
ODOO_USERNAME=your_username
ODOO_PASSWORD=your_password

# ====================================
# Server Configuration
# ====================================
SERVER_MODE=http
HOST=0.0.0.0
PORT=5000

# Public server URL (important for OAuth with ChatGPT)
# Examples:
#   Easypanel: https://odoo-mcp.yourdomain.easypanel.host
#   Replit: https://your-repl.replit.dev
#   Custom: https://mcp.yourdomain.com
# Leave empty for auto-detection from REPLIT_DOMAINS or localhost fallback
SERVER_URL=

# Authentication (for HTTP mode)
# Generate a secure secret key: openssl rand -hex 32
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# API Keys (comma separated, for HTTP mode)
# These provide direct access without login
# IMPORTANT: Keep these secret and use strong, random keys in production
API_KEYS=test_key_123,another_key_456

# Login Authentication:
# The /login endpoint validates against ODOO_USERNAME and ODOO_PASSWORD
# This means users must have valid Odoo credentials to obtain JWT tokens

# Redis Cache (optional)
REDIS_ENABLED=false
REDIS_URL=redis://localhost:6379/0
CACHE_TTL=300

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json

# Rate Limiting (requests per minute)
RATE_LIMIT=60

# Odoo Connection Settings
ODOO_TIMEOUT=30
ODOO_MAX_RETRIES=3
